# ============================================
# FUND-OS v5.0 依赖清单
# 安装: pip install -r requirements.txt
# ============================================

# ---- Web 框架 ----
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0
gevent>=23.9.0

# ---- 数据处理 ----
pandas>=2.1.0
numpy>=1.25.0

# ---- 数据库 ORM ----
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9          # PostgreSQL 适配器（生产）
# sqlite3 是 Python 内置，无需安装

# ---- 缓存 ----
redis>=5.0.0                    # Redis 客户端（可选）

# ---- 认证安全 ----
PyJWT>=2.8.0                    # JWT Token
cryptography>=41.0.0            # 加密/哈希
python-dotenv>=1.0.0            # 环境变量管理

# ---- 金融数据源 ----
tushare>=1.2.89                 # A 股 / 基金数据
yfinance>=0.2.18                # 海外市场数据

# ---- 可视化 ----
matplotlib>=3.7.0
seaborn>=0.12.0
openpyxl>=3.1.0                 # Excel 导出

# ---- 监控与日志 ----
prometheus-client>=0.19.0       # Prometheus 指标暴露
structlog>=24.1.0               # 结构化日志

# ---- 工具库 ----
python-dateutil>=2.8.0
requests>=2.31.0
httpx>=0.26.0                   # 现代 HTTP 客户端（可选）

# ---- 支付二维码生成 ----
qrcode>=7.4.2                   # 生成真实可扫描的支付二维码
Pillow>=10.0.0                  # qrcode 生成 PNG 的图像后端

# ---- 开发/测试工具（可选）----
# pytest>=7.4.0
# pytest-cov>=4.1.0
# black>=23.0.0
# flake8>=6.1.0
